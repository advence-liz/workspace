{
  "name": "template",
  "version": "1.0.0",
  "description": "a webpack template ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node `npm bin`/webpack-dev-server --content-base=build --host 10.11.6.185 --https",
    "dev": "nodemon   `npm bin`/webpack-dev-server --content-base=build --color",
    "start": "webpack-dev-server --content-base=build --color",
    "build": "webpack"
  },
  "keywords": [
    "webpack"
  ],
  "author": "advence-liz",
  "license": "ISC",
  "nodemonConfig": {
    "watch": [
      "webpack.*.js",
      ".qsrc.json"
    ]
  },
  "devDependencies": {
    "lilith-cli": "^1.2.9",
    "lilith-compiler": "^1.2.6"
  },
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-motion": "^0.5.2"
  }
}
